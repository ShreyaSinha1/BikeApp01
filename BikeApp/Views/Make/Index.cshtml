
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
<div class="row">
    <div class="col-sm-6">
        <h2 class="text-info">Make Vechiles</h2>
    </div>
    <div class="col-sm-6">
        <a class="btn btn-info" asp-action="Create">Add New Make</a>
    </div>
    <div>
        <table class="table table-striped">
            <tr class="table-info">
                <th>
                    @*@Html.DisplayNameFor(m=>m.Name)*@
                </th>
                <th>

                </th>
            </tr>
            <tr>
                @foreach (var item in Model)
                {
                    <td>
                        @*@Html.DisplayFor(y=>item.)*@
                    </td>
                    <td>
                        @*<a type="button" class="btn btn-primary"
                   href="@Url.Action("Edit/"+item.Id)">Edit</a>|
                <a type="button" class="btn btn-primary"
                         href="@Url.Action("Delete/"+item.Id)">Delete</a>*@
                        <form method="post" action="Delete">
                            <input onclick="if(!confirm('Do you want to delete?')){return false;}" type="button" asp-action="Delete" asp-route-id="@item.Id" />
                        </form>
                    </td>
                }
                @if (ApplicationUser.IsInRole(Roles.Admin) || ApplicationUser.IsInRole(Roles.Executive))
                {
                    <partial name="_TableButtonPartials" model="item.Id"></partial>
                }
                else
                {
                    <partial name="_TableButtonPartials" model="item.Id"></partial>
                }
            </tr>
        </table>
    <partial name="_PaginationPartial" view-data="ViewData"
             model="Model">
        </partial></div>
</div>
